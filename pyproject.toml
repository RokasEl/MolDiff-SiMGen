[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "moldiff-simgen"
version = "0.1"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "easydict",
]
